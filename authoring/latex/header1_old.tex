% --------------------------------------------------
% Margini pagina
% --------------------------------------------------
\usepackage[
  a4paper,
  left=1.5cm,
  right=1.5cm,
  top=1.5cm,
  bottom=1.5cm
]{geometry}

% --------------------------------------------------
% Immagini
% --------------------------------------------------
\usepackage{graphicx}

% --------------------------------------------------
% Font per XeLaTeX
% --------------------------------------------------
\usepackage{fontspec}
\setmainfont{Times New Roman}

% --------------------------------------------------
% Paragrafi
% --------------------------------------------------
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% --------------------------------------------------
% Link
% --------------------------------------------------
\usepackage{hyperref}

% --------------------------------------------------
% Sfondo pagina
% --------------------------------------------------
\usepackage{background}
\backgroundsetup{
  scale=1,
  angle=0,
  opacity=1,
  position=current page.center,
  pages=all,
  contents={
    \includegraphics[
      width=\paperwidth,
      height=\paperheight
    ]{./authoring/imgs/sfondocopyright.png}
  }
}